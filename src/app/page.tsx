export default function Home() {
  // The middleware handles redirection, so this component can be minimal.
  // It will redirect to /login or /dashboard based on authentication state.
  return null;
}
